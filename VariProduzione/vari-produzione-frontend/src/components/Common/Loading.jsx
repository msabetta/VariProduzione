import React from 'react';

const Loading = ({ color, size }) => {
    return (
        <div
            style={{
                backgroundColor: color,
            }}
        >
        </div>
    );
};

export default Loading;